# Screenshots Placeholder

Suggested screenshots to add after running the stack:

- FastAPI Swagger UI at `/docs`.
- Prometheus target status for the API scrape.
- Grafana dashboard panels for traffic and latency.
- Example batch output CSV.

